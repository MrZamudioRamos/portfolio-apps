import { useColors, useTheme } from '@portfolio/ui';
import { useCollection } from '@portfolio/storage';
import { Ionicons } from '@expo/vector-icons';
import { useRouter } from 'expo-router';
import React, { useMemo } from 'react';
import { Alert, Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import type { Plant } from '../src/models/plant';
import { useActiveGarden } from '../src/hooks/useActiveGarden';
import { goBackOr } from '../src/utils/navigation';

export default function LightMeterScreen() {
  const colors = useColors();
  const { spacing, fontSize, fontWeight, radii } = useTheme();
  const router = useRouter();
  const { activeGarden } = useActiveGarden();
  const plants = useCollection<Plant>('plants');
  const gardenPlants = useMemo(() => plants.items.filter((plant) => !plant.deletedAt && (!activeGarden?.id || plant.gardenId === activeGarden.id)), [activeGarden?.id, plants.items]);
  const firstPlantId = gardenPlants[0]?.id;
  const s = useMemo(() => makeStyles(spacing, fontSize, fontWeight, radii), [fontSize, fontWeight, radii, spacing]);

  return <SafeAreaView style={[s.container, { backgroundColor: colors.background }]} edges={['top']}>
    <View style={[s.header, { borderBottomColor: colors.border }]}><Pressable accessibilityRole="button" accessibilityLabel="Mi Huerto" onPress={() => goBackOr(router)} style={s.headerButton}><Ionicons name="chevron-back" size={22} color={colors.text} /><Text style={[s.headerBack, { color: colors.text }]}>Mi Huerto</Text></Pressable><View style={{ width: 44 }} /></View>
    <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={s.scroll}>
      <View style={s.titleRow}><View style={{ flex: 1 }}><Text style={[s.title, { color: colors.text }]}>Medidor Solar</Text><Text style={[s.status, { color: colors.textSecondary }]}>Sin sensor conectado</Text></View><Pressable accessibilityRole="button" accessibilityLabel="Calibrar sensor" onPress={() => Alert.alert('Sensor no disponible', 'Esta versión todavía no tiene acceso a un sensor de luz del dispositivo. No se guardará ninguna lectura inventada.')} style={[s.iconButton, { backgroundColor: colors.surfaceAlt, borderColor: colors.border }]}><Ionicons name="options-outline" size={20} color={colors.primary} /></Pressable></View>
      <View style={[s.liveCard, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={s.liveTop}><View style={[s.liveIcon, { backgroundColor: colors.surfaceAlt }]}><Ionicons name="sunny-outline" size={22} color={colors.textSecondary} /></View><View style={{ flex: 1 }}><Text style={[s.liveKicker, { color: colors.textSecondary }]}>Lectura actual</Text><Text style={[s.liveHeading, { color: colors.text }]}>No disponible</Text><Text style={[s.liveNow, { color: colors.textSecondary }]}>Conecta un sensor compatible para medir lux y orientación</Text></View></View><View style={s.emptyReading}><Ionicons name="analytics-outline" size={34} color={colors.textDisabled} /><Text style={[s.emptyReadingTitle, { color: colors.text }]}>Sin datos medidos</Text><Text style={[s.emptyReadingText, { color: colors.textSecondary }]}>No mostramos valores de luz, horas de sol ni dirección hasta tener una lectura real.</Text></View></View>
      <Text style={[s.sectionTitle, { color: colors.text }]}>Ubicación registrada</Text>
      <View style={[s.locationCard, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={s.locationHeading}><Ionicons name="location-outline" size={20} color={colors.primary} /><Text style={[s.locationTitle, { color: colors.text }]}>{activeGarden ? `${activeGarden.name} · ${activeGarden.province}` : 'Sin huerto seleccionado'}</Text></View><View style={[s.zone, { borderTopColor: colors.border }]}><Ionicons name="information-circle-outline" size={20} color={colors.primary} /><View style={{ flex: 1 }}><Text style={[s.zoneTitle, { color: colors.text }]}>{gardenPlants.length ? `${gardenPlants.length} plantas registradas` : 'Aún no hay plantas registradas'}</Text><Text style={[s.zoneDescription, { color: colors.textSecondary }]}>{gardenPlants.length ? 'El medidor podrá ayudarte a documentar la luz de estas plantas cuando exista una lectura real.' : 'Añade una planta y configura su ubicación para completar este espacio.'}</Text></View></View></View>
      <View style={[s.ruleCard, { backgroundColor: colors.surfaceAlt, borderColor: colors.border }]}><Ionicons name="water-outline" size={21} color={colors.primary} /><View style={{ flex: 1 }}><Text style={[s.ruleTitle, { color: colors.text }]}>Regla de Semilla</Text><Text style={[s.ruleText, { color: colors.textSecondary }]}>Antes de regar, introduce el dedo 2 cm en el sustrato. Esta recomendación no depende de una lectura de luz.</Text></View></View>
      <View style={s.actions}><Pressable accessibilityRole="button" style={[s.primaryButton, { backgroundColor: colors.primary }]} onPress={() => router.push({ pathname: '/entry/new', params: { ...(firstPlantId ? { plantId: firstPlantId } : {}), type: 'note' } } as any)}><Ionicons name="create-outline" size={18} color="#fff" /><Text style={[s.primaryButtonText, { color: '#fff' }]}>Registrar observación manual</Text></Pressable><Pressable accessibilityRole="button" disabled style={[s.outlineButton, { borderColor: colors.border, opacity: 0.7 }]}><Ionicons name="bookmark-outline" size={18} color={colors.textSecondary} /><Text style={[s.outlineButtonText, { color: colors.textSecondary }]}>Guardar medición automática</Text></Pressable><Pressable accessibilityRole="button" style={[s.outlineButton, { borderColor: colors.border }]} onPress={() => Alert.alert('Historial solar', 'Todavía no hay mediciones automáticas guardadas.') }><Ionicons name="time-outline" size={18} color={colors.primary} /><Text style={[s.outlineButtonText, { color: colors.primary }]}>Historial solar</Text></Pressable><Pressable accessibilityRole="button" style={[s.outlineButton, { borderColor: colors.border }]} onPress={() => router.push('/garden/map' as any)}><Ionicons name="map-outline" size={18} color={colors.primary} /><Text style={[s.outlineButtonText, { color: colors.primary }]}>Configurar mapa del huerto</Text></Pressable></View>
    </ScrollView>
  </SafeAreaView>;
}

const makeStyles = (spacing: Record<string, number>, fontSize: Record<string, number>, fontWeight: ReturnType<typeof useTheme>['fontWeight'], radii: Record<string, number>) => StyleSheet.create({
  container: { flex: 1 }, header: { minHeight: 56, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: spacing.lg, borderBottomWidth: StyleSheet.hairlineWidth }, headerButton: { minHeight: 44, flexDirection: 'row', alignItems: 'center', gap: 4 }, headerBack: { fontSize: fontSize.sm, fontWeight: fontWeight.semibold }, scroll: { padding: spacing.lg, paddingBottom: 40 }, titleRow: { flexDirection: 'row', alignItems: 'center', marginBottom: spacing.md }, title: { fontSize: fontSize.xl, fontWeight: fontWeight.bold }, status: { fontSize: fontSize.sm, fontWeight: fontWeight.semibold, marginTop: 2 }, iconButton: { width: 44, height: 44, alignItems: 'center', justifyContent: 'center', borderWidth: 1, borderRadius: radii.md }, liveCard: { borderWidth: 1, borderRadius: radii.xl, padding: spacing.lg, marginBottom: spacing.xl }, liveTop: { flexDirection: 'row', alignItems: 'flex-start', gap: spacing.md }, liveIcon: { width: 44, height: 44, borderRadius: radii.md, alignItems: 'center', justifyContent: 'center' }, liveKicker: { fontSize: fontSize.xs }, liveHeading: { fontSize: fontSize.md, fontWeight: fontWeight.bold, marginTop: 2 }, liveNow: { fontSize: fontSize.xs, lineHeight: 18, marginTop: 3 }, emptyReading: { alignItems: 'center', borderTopWidth: StyleSheet.hairlineWidth, borderTopColor: '#d8e2d4', marginTop: spacing.lg, paddingTop: spacing.lg }, emptyReadingTitle: { fontSize: fontSize.md, fontWeight: fontWeight.bold, marginTop: spacing.sm }, emptyReadingText: { textAlign: 'center', fontSize: fontSize.sm, lineHeight: 20, marginTop: 4 }, sectionTitle: { fontSize: fontSize.lg, fontWeight: fontWeight.bold, marginBottom: spacing.sm }, locationCard: { borderWidth: 1, borderRadius: radii.xl, overflow: 'hidden' }, locationHeading: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm, padding: spacing.md }, locationTitle: { fontSize: fontSize.md, fontWeight: fontWeight.bold }, zone: { flexDirection: 'row', gap: spacing.sm, borderTopWidth: StyleSheet.hairlineWidth, padding: spacing.md }, zoneTitle: { fontSize: fontSize.sm, fontWeight: fontWeight.bold }, zoneDescription: { fontSize: fontSize.xs, lineHeight: 18, marginTop: 5 }, ruleCard: { flexDirection: 'row', alignItems: 'flex-start', gap: spacing.sm, borderWidth: 1, borderRadius: radii.lg, padding: spacing.md, marginTop: spacing.lg }, ruleTitle: { fontSize: fontSize.sm, fontWeight: fontWeight.bold }, ruleText: { fontSize: fontSize.xs, lineHeight: 18, marginTop: 4 }, actions: { gap: spacing.sm, marginTop: spacing.lg }, primaryButton: { minHeight: 52, borderRadius: radii.md, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: spacing.sm }, primaryButtonText: { fontSize: fontSize.sm, fontWeight: fontWeight.bold }, outlineButton: { minHeight: 48, borderRadius: radii.md, borderWidth: 1, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: spacing.sm }, outlineButtonText: { fontSize: fontSize.sm, fontWeight: fontWeight.semibold },
});

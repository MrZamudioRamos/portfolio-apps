/**
 * Native route name used by Stitch's implementation contract.
 * The approved UI lives in plant/new and remains the single source of truth
 * for validation, photo permissions and plant creation.
 */
export { default } from '../plant/new';
